# TODO: Orchestrator — routes player messages between agents.
# Receives raw player input + campaign state, decides which agents to invoke,
# returns a final response dict compatible with the existing server.py contract.
